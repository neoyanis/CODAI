function runSimulation(logic, code) {
  console.log("Simulation running:", logic.type);

  if (logic.type === "adder") {
    alert("✅ Simulation OK : addition logique correcte");
  } else {
    alert("⚠️ Simulation non disponible");
  }
}
